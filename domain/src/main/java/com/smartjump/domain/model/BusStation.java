package com.smartjump.domain.model;

/**
 *
 */
public class BusStation extends Jump {

    public BusStation(int id, float latitude, float longitude, float distance, String address) {
        super(id, latitude, longitude, distance, address);
    }
}
